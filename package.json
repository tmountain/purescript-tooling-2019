{
  "scripts": {
    "postinstall": "spago install",
    "build": "spago build",
    "clean": "rm -rf node_modules output .spago .cache dist/* *.lock",
    "bundle": "spago bundle-app --main Main --to dist/app.js && parcel build assets/index.html",
    "pscid:build": "spago bundle-app --main Main --to dist/app.js && parcel build assets/index.html"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "parcel-bundler": "^1.12.4",
    "pscid": "^2.8.5",
    "purescript": "^0.13.3",
    "purty": "^4.5.1",
    "spago": "^0.10.0"
  },
  "name": "purescript-tooling-2019",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT"
}
